---
paths:
  - "docs/cli/**"
---
# CLI Reference

Documentación sobre comandos y flags de Claude Code CLI.

## Temas
- Comandos principales (`claude`, `claude -p`, `claude -c`)
- Flags útiles (`--model`, `--output-format`, `--allowedTools`)
- Modo headless para scripts y CI/CD
- Gestión de sesiones (`--resume`, `--continue`)
- LSP (Language Server Protocol) para navegación semántica

## Archivos
- `cli-vs-plugins.md` - Comparación CLI tools vs plugins
- `session-management.md` - Gestión de sesiones
- `lsp.md` - Language Server Protocol

## Fuente principal
https://code.claude.com/docs/en/cli-reference
